# Email configuration
SMTP_SERVER=
SMTP_PORT=587
SMTP_LOGIN=
SMTP_PASSWORD=
FROM_EMAIL=
FROM_NAME=

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=root
DB_NAME=go_crm   
DATABASE_URL=postgres://postgres:root@localhost:5432/go_crm?sslmode=disable


# OAuth2 configuration
FACEBOOK_CLIENT_ID=your-client-id
FACEBOOK_CLIENT_SECRET=your-client-secret
FACEBOOK_REDIRECT_URL=http://localhost:8080/auth/facebook/callback

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URL=http://localhost:8080/auth/google/callback

GITHUB_CLIENT_ID=your-client-id
GITHUB_CLIENT_SECRET=your-client-secret
GITHUB_REDIRECT_URL=http://localhost:8080/auth/github/callback

TWITTER_CLIENT_ID=your-client-id
TWITTER_CLIENT_SECRET=your-client-secret
TWITTER_REDIRECT_URL=http://localhost:8080/auth/twitter/callback

LINKEDIN_CLIENT_ID=your-client-id
LINKEDIN_CLIENT_SECRET=your-client-secret
LINKEDIN_REDIRECT_URL=http://localhost:8080/auth/linkedin/callback

# Application configuration
APP_PORT=8080
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8080

# JWT configuration
JWT_SECRET=supersecretkey
JWT_EXPIRATION_HOURS=72 

# Logging configuration
LOG_LEVEL=info
LOG_FILE=app.log

# Other configurations
TIMEZONE=UTC
CACHE_EXPIRATION_MINUTES=60
MAX_UPLOAD_SIZE_MB=10
ALLOWED_FILE_TYPES=jpg,png,pdf,docx
SESSION_SECRET=your_session_secret_key
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
REDIS_DB=0

# Third-party service configurations
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_SECRET=your_paypal_secret
STRIPE_API_KEY=your_stripe_api_key
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your_s3_bucket_name
GOOGLE_ANALYTICS_ID=your_google_analytics_id
SENTRY_DSN=your_sentry_dsn

# Feature toggles
FEATURE_X_ENABLED=true
FEATURE_Y_ENABLED=false
BETA_FEATURES_ENABLED=false


# Note: Replace placeholder values (e.g., your_db_user, your_jwt_secret_key) with actual configuration values before using the application.
# Ensure sensitive information is kept secure and not hard-coded in the source code.
# This .env file is for local development. For production, use environment variables or a secure secrets manager.
# Do not commit this file to version control if it contains sensitive information.
# Use .env.example as a template for creating your own .env file.
# For more information on configuring the application, refer to the documentation.
# Keep this file updated with any new configuration options added to the application.
# Always back up your .env file before making changes.
# If you encounter issues, check the .env file for typos or incorrect values.
# Consider using a library to manage environment variables and configuration settings.
# Regularly review and update your configuration settings for security and performance.


